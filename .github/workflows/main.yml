name: My First GitHub Action

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
    - name: Run a one-line script
      run: echo "Hello, the CI/CD pipeline is working!"

    # THIS STEP WILL BREAK THE PIPELINE
    - name: Run a broken command
      run: this-is-not-a-real-command
